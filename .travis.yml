language: ruby
install: bundle install --without assets development production
rvm:
  - 1.9.3
env:
  - DB=sqlite
script: 
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rake test
bundler_args: --binstubs=./bundler_stubs
