<h1>Songs</h1>

<table class="table table-striped table-bordered table-condensed">
	  <thead>
  <tr>
    <th>Artist</th>
    <th>URL</th>
  </tr>
  </thead>
  <tbody>
<% @results['results']['artistmatches']['artist'].each do |artist| %>

	<tr>
		<td><%= artist['name'] %></td>
		<td><%= artist['url'] %></td>
	</tr>
		
<% end %>
 </tbody>
</table>